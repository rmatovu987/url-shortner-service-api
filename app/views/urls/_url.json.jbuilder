json.call(url, :id, :original_url, :short_url, :clicks, :user_id)

# json.id url.id
# json.original_url url.original_url
# json.short_url url.short_url
# json.clicks url.clicks
# json.user_id url.user_id
